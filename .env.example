# Required: Anthropic API key (https://console.anthropic.com/settings/keys)
ANTHROPIC_API_KEY=

# Optional: override the default Claude model
ANTHROPIC_MODEL=claude-haiku-4-5

# Optional: HTTP port (default 3000)
PORT=3000
